Readme for Project 3 of CMP_SCI-4760 Spring 2025 for Logan Bessinger
Make the file using the make file provided and "make" command.
use file with command "./oss -n (x) -s (y) -t (z) -i(a) -f(file)" where (x), (y), (a), and (z) are your prefered intigers while (file) is a string/name for your output file name.
or use "./oss -h" for help
git repository for this project is in
https://github.com/Axolotl2277/OSP3
Versons are in the different Branches